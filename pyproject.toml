[project]
name = "continuity-break-detector"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx",
]

[project.optional-dependencies]
test = [
  "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-s"
